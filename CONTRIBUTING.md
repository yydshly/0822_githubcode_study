# 参与约定

## 新建研究项目

1. 先创建研究提案 Issue，写清原项目地址、研究问题和预期证据。
2. 从 `projects/_template/` 复制项目目录，目录名使用小写 kebab-case。
3. 先记录研究边界与许可证，再引入第三方代码或素材。
4. 实验过程持续追加到 `NOTES.md`，不要只保留成功结果。
5. 更新顶层 README、项目目录和 Pages 展厅入口。

## 提交建议

- 每次提交只表达一个清晰变化。
- 提交信息优先使用 `study:`、`feat:`、`docs:`、`test:`、`fix:`、`archive:` 等前缀。
- 不提交密钥、个人数据、依赖目录和可重新生成的大型产物。
- 引入上游源码时保留其许可证、版权说明和来源链接。

## 完成标准

进入 `validated` 或 `archived` 前，应至少写明：

- 研究问题和适用边界；
- 可复现步骤与运行环境；
- 支撑结论的证据；
- 已知限制、失败案例和后续重启条件。

