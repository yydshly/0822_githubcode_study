# 水中套圈场景原型

## 目标

基于固定版本 Particles4All，验证局部液体—刚体能力能否转化为可玩的网页水压套圈游戏，而不是继续扩展宏观水场景。

在线入口：`docs/demos/particles4all/water-ring-game/`

## 能力归属

| 层级 | 实现 |
| --- | --- |
| `U0` 源库 | PBF 水体、torus 刚体、Shape Matching、浮沉、碰撞、SSFR |
| `E1` 扩展 | Runtime Adapter 流体包注入、持续水泵、圆环位姿采样、中心与孔轴目标约束 |
| `G1` 游戏 | 单次水压、单杆杆口、挂接状态机、计分、键盘操作、引导演示 |

## 当前验收

1. 页面必须运行固定上游 WebGPU Runtime，而不是独立水动画。
2. 五个圆环必须来自源库原生 `torus` 形状。
3. 每次按钮操作必须增加求解器内流体粒子，并推动圆环产生可测位移。
4. 页面必须显示新增粒子数、最大抬升和最大位移。
5. 圆环进入杆口后，必须可见地完成对准、沿杆下落并稳定停留，不能只在宽泛区域内计分。
6. 通关时必须保持同一个源库 torus 刚体，且其中心接近单杆挂接位置；不能用独立 DOM 圆环替换。
7. 重置、SSFR/particles 切换、键盘 A/S/D/R 和 WebGPU 错误提示必须可用。
8. 持续水泵必须循环注入求解器流体，且在圆环进入杆口或达到安全循环上限后自动停止。
9. 挂接圆环的孔轴必须与单杆方向对齐，避免中心到位但视觉上仍未穿杆。
10. Runtime 必须在圆环初始位姿读取完成后才启用水泵，禁止出现 `ready` 但无目标圆环的启动竞态。
11. 1366×768 桌面视口中，顶栏不得覆盖游戏标题，水泵按钮和实时阶段进度必须可见。
12. 连续重置并启动三局均须在 20 秒内通关；运行事件必须包含 `game-ready`、`pump-start`、`fluid-pulse` 和 `game-complete`。

## 下一 Gate

当前已完成“一个圆环、一个单杆”的完整可玩回合：水泵锁定一个目标环，最多注入 12 次位置自适应真实水脉冲；进入杆口后由 Runtime 刚体捕获约束完成对准、穿杆、下落和保持。若 12 次后圆环已有可测水动力位移但随机漂移未进入杆口，游戏层会进入杆口导向，以保证不同性能设备上仍可完成。

下一 Gate 是把游戏约束替换为真实静态细杆碰撞：验证杆体粒子采样、圆环孔洞接触、摩擦与稳定挂接。在该 Gate 通过前，不把当前结果描述为完全物理穿杆模拟。
